Fix the Markets page without breaking existing wallet connection, paper trading, portfolio, or routing functionality.

1. PRICE / MARKET CAP DISPLAY
The current token list is confusing because raw token prices are showing in scientific notation, such as $1.18e-5.

Do not show scientific notation anywhere.

For the main column, show Market Cap instead of Price.

Use DexScreener fields:
- pair.marketCap when available
- fallback to pair.fdv when marketCap is unavailable

Format market cap as readable shorthand:
- $50K
- $120K
- $1.2M
- $15M

If showing token price, show it as a smaller secondary line under the token name:
- $0.0000118
- $0.00000042

Never show values like e-5 or e-8.

2. MORE TOKENS BY DEFAULT
The Markets page should not look empty before the user searches.

Populate the Markets page with more default Solana tokens.

Use:
- DexScreener token boosts endpoint:
  GET https://api.dexscreener.com/token-boosts/top/v1
- Filter results to chainId === "solana"
- Extract tokenAddress values
- Hydrate those token addresses using:
  GET https://api.dexscreener.com/tokens/v1/solana/{comma_separated_token_addresses}
- DexScreener supports multiple comma-separated token addresses, up to 30 per request.

Show at least 20-30 tokens per tab where possible.

For New Launches:
- Make sure PumpPortal WebSocket subscribeNewToken is connected.
- Show new token launches in real time if data is available.
- If PumpPortal has not returned data yet, show a clean loading or empty state, not a broken/empty layout.

3. FORMAT ALL NUMBERS PROPERLY
Create shared formatting helpers and use them everywhere:

formatMarketCap(value):
- 50000 -> $50K
- 1200000 -> $1.2M
- 15000000 -> $15M

formatPrice(value):
- 0.0000118 -> $0.0000118
- 0.25 -> $0.25
- never scientific notation

formatVolume(value):
- same shorthand style as market cap

formatPercent(value):
- 12.34 -> +12.34%
- -8.9 -> -8.90%

4. DATA LABEL AUDIT
Audit all token data mapping:
- priceUsd or calculated token price = Price
- marketCap or fdv = Market Cap
- volume.h24 = 24h Volume
- priceChange.h24 = 24h Change

Labels must match the actual data exactly.

Preserve all existing functionality.