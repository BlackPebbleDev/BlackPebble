BLACKPEBBLE SAFE CLEANUP PASS V1

IMPORTANT

Work ONLY on the current branch:

repo-cleanup-safe-v1

DO NOT:
- publish
- deploy
- merge to main
- modify UI
- redesign pages
- change trading logic
- change portfolio calculations
- change leaderboard calculations
- change callout logic
- change thesis logic
- change journal logic
- change admin functionality

Goal:

Safely clean up BlackPebble based on the completed repository audit.

====================================================
PHASE 1 — SECURITY HARDENING
====================================================

Review and safely address the audit findings.

1. SQLite Files

Audit found:

artifacts/data/
contains tracked SQLite files.

Actions:

- Determine whether files are stale and no longer used.
- Do NOT permanently delete immediately if uncertain.
- Add proper database file ignores to .gitignore:
  *.sqlite
  *.sqlite3
  *.db

- Document findings in:
  docs/SECURITY_REVIEW.md

Include:
- filenames found
- whether they appear unused
- whether they contain historical data
- recommended next action

2. CORS Configuration

Audit found:

cors({
 origin: true,
 credentials: true
})

Actions:

- Restrict CORS safely.
- Allow:
  https://blackpebble.fun

- Allow development environments.
- Do not break authentication.
- Use environment-based configuration if appropriate.

3. JWT Secret Validation

Audit found:

JWT_SECRET missing can silently fail auth.

Actions:

- Add startup validation.
- Production should clearly fail if required auth secrets are missing.
- Development should show clear warnings.

4. Security Headers

Actions:

- Add safe Helmet configuration if compatible.
- Do not break existing functionality.

5. Rate Limiting

Actions:

- Add basic protection for public endpoints.
- Keep limits reasonable.
- Do not impact normal users.

====================================================
PHASE 2 — DEAD WEIGHT REMOVAL
====================================================

Audit found:

- 23 unused shadcn wrappers
- 15 unused npm packages
- unused Radix dependencies
- scripts/src/hello.ts placeholder
- recharts unused

Actions:

Only remove items that are confirmed unused.

Before removing:

- search entire repository
- verify zero imports

If uncertain:
leave it alone.

Do NOT remove anything that could impact production.

====================================================
PHASE 3 — DOCUMENTATION
====================================================

Update:

docs/REPO_AUDIT.md
docs/SECURITY_REVIEW.md
docs/CLEANUP_LOG.md

Document:

- what was removed
- what was not removed
- why
- any future recommendations

====================================================
PHASE 4 — TESTING
====================================================

After cleanup:

Run:

- install
- build
- typecheck
- lint (if available)

Verify:

- Trading page loads
- Markets page loads
- Portfolio page loads
- Feed page loads
- Leaderboard page loads
- Utilities page loads
- Admin page loads
- Token page loads
- Search still works

If anything fails:

revert that specific cleanup item.

====================================================
FINAL REPORT
====================================================

Provide:

1. Files changed
2. Packages removed
3. Security fixes applied
4. CORS changes
5. JWT changes
6. SQLite findings
7. Build results
8. Risks remaining
9. Recommendations before merging

IMPORTANT:

Favor safety over cleanup.

If uncertain about deleting anything:
leave it in place and document it instead.