BlackPebble mobile polish, X identity planning, social previews, and market UI improvements

Do not break any existing functionality.

Preserve:
- Wallet connection
- Paper trading
- Token search
- Markets page
- Trading Desk
- Portfolio
- Existing backend APIs
- Existing database data
- Existing Replit deployment behavior

Before editing, inspect the current files responsible for:
- Markets page token table/list
- Trading Desk token list/cards
- Mobile responsive CSS/layout
- Header/wallet button
- HTML metadata / Open Graph tags
- Authentication/wallet state

Make the following improvements only.

━━━━━━━━━━━━━━━━━━━━━━
1. Fix mobile token list layout
━━━━━━━━━━━━━━━━━━━━━━

Current problem:
On mobile, users must scroll horizontally to view Market Cap and 24h stats.

Goal:
No horizontal scrolling anywhere on mobile.

For screens under 768px:

Convert token tables into responsive mobile rows/cards.

Each token row should display:

LEFT:
- Rank
- Token logo
- Token symbol (bold)
- Token name
- Token price (small muted text)

RIGHT:
- Market Cap
- 24h Change

Example:

DAFLE
Darlen Fleur
$0.0004295

$411.9K MC
+3256.00%

Requirements:
- No clipped text
- No sideways scrolling
- No overflowing columns
- Apply consistently to:
  - Markets
  - Trading Desk
  - Search results
  - Trending lists
  - Any token discovery screens

Desktop layout may remain table-based.

━━━━━━━━━━━━━━━━━━━━━━
2. Market Cap becomes primary display value
━━━━━━━━━━━━━━━━━━━━━━

Current problem:
Users are seeing tiny token prices in scientific notation.

Goal:
Market Cap becomes the primary discovery metric.

Use:

pair.marketCap

Fallback:

pair.fdv

Display:

$8.7K MC
$76.4K MC
$411.9K MC
$1.72M MC
$3.96M MC

Never display:

1.18e-5
4.47e-4
1.39e-3

Create reusable formatting helpers:

formatMarketCap()
formatPrice()
formatVolume()
formatPercent()

Apply globally.

━━━━━━━━━━━━━━━━━━━━━━
3. Clean number formatting everywhere
━━━━━━━━━━━━━━━━━━━━━━

Prices:

$1.18e-5 → $0.0000118
$4.47e-4 → $0.000447
$1.39e-3 → $0.00139

Market Caps:

$50.2K
$1.3M
$15.4M

Volumes:

$8.4K
$122K
$2.8M

Never show scientific notation anywhere on the site.

━━━━━━━━━━━━━━━━━━━━━━
4. Improve header and mobile experience
━━━━━━━━━━━━━━━━━━━━━━

Keep wallet connection.

Do NOT remove wallet support.

Do NOT replace wallet login.

However:

Current wallet button is oversized and dominates the mobile screen.

Improve:

- Smaller cleaner BlackPebble logo
- Better spacing
- More professional alignment
- Compact wallet connect button
- Black / graphite / gold aesthetic
- Less visual clutter
- Better use of vertical space

Keep wallet functionality exactly as it works today.

━━━━━━━━━━━━━━━━━━━━━━
5. Add X login architecture
━━━━━━━━━━━━━━━━━━━━━━

Remove Telegram plans completely.

Goal:

Allow users to eventually sign in with X so:

- X profile picture can appear on leaderboards
- X username can appear on leaderboards
- Future PnL cards can display X identity
- Future social sharing features can use X profile data

Security requirements:

- Do NOT ask for X passwords
- Do NOT store passwords
- Do NOT create fake OAuth
- Do NOT simulate X accounts
- Do NOT weaken wallet authentication

Wallet connection remains primary.

Add:

"Login with X"

as a secondary option.

If OAuth is not fully configured:

Display:

"Login with X (Coming Soon)"

and safely scaffold the architecture.

Database planning:

users
- id
- display_name
- avatar_url
- created_at
- last_active

user_identities
- user_id
- provider
- provider_user_id
- wallet_address nullable
- x_username nullable
- created_at

Future users should be able to have:

Wallet Identity
+
X Identity

linked to the same profile.

Do not perform risky migrations.

Do not break current paper trading accounts.

━━━━━━━━━━━━━━━━━━━━━━
6. Add social preview metadata
━━━━━━━━━━━━━━━━━━━━━━

Goal:

When blackpebble.fun is pasted into:

- iMessage
- X
- Telegram
- Discord

show a professional preview card.

Use the BlackPebble logo/banner as:

public/opengraph.jpg

Recommended size:

1200x630

Add/update:

<title>BlackPebble</title>

<meta name="description" content="Practice trading live Solana tokens with paper money. A professional Solana paper trading and market analytics platform.">

<meta property="og:title" content="BlackPebble">
<meta property="og:description" content="Practice trading live Solana tokens with paper money. Track markets, test trades, and sharpen your strategy risk-free.">
<meta property="og:image" content="https://blackpebble.fun/opengraph.jpg">
<meta property="og:url" content="https://blackpebble.fun">
<meta property="og:type" content="website">

<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="BlackPebble">
<meta name="twitter:description" content="Practice trading live Solana tokens with paper money. Track markets, test trades, and sharpen your strategy risk-free.">
<meta name="twitter:image" content="https://blackpebble.fun/opengraph.jpg">

Also update favicon using the BlackPebble logo if possible.

━━━━━━━━━━━━━━━━━━━━━━
7. Future PnL card planning
━━━━━━━━━━━━━━━━━━━━━━

Do not fully build this yet.

Only create a placeholder/TODO architecture.

Future card should support:

- BlackPebble branding
- User avatar
- X username
- Entry price
- Exit price
- PnL %
- PnL $
- X multiplier

Examples:

+248%
+4.2X

or

-35%
0.65X

Future cards should be designed for sharing on X.

Do not implement sharing logic yet.

━━━━━━━━━━━━━━━━━━━━━━
8. Verification checklist
━━━━━━━━━━━━━━━━━━━━━━

After changes:

- Run app successfully
- Verify mobile token lists fit screen width
- Verify no horizontal scrolling
- Verify Market Cap is visible
- Verify 24h change is visible
- Verify wallet connection still works
- Verify token search still works
- Verify Markets page still works
- Verify Trading Desk still works
- Verify no scientific notation exists anywhere
- Verify no fake X login is active
- Verify Open Graph image exists
- Verify deployment still succeeds

Do not remove any existing working functionality.