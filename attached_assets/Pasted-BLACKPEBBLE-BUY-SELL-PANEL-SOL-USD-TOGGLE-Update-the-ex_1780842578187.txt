BLACKPEBBLE BUY/SELL PANEL SOL/USD TOGGLE

Update the existing token Buy/Sell trading panel.

Goal:
Let users enter paper trade amounts in either SOL or USD while keeping the backend/API trade execution compatible with the existing SOL-based trade amount.

Do not rebuild the trading panel from scratch.

Do not modify:

* portfolio calculations except display formatting in the panel
* leaderboard calculations
* market feeds
* auth
* database schema
* SOL Recovery
* full Trade Planner utility
* Mini Trade Planner except where needed for clean unit sync

IMPORTANT:
Existing paper buy/sell behavior must continue working exactly as before in SOL mode.

====================================================

1. AMOUNT UNIT TOGGLE
    ====================================================

Inside the Buy/Sell panel near the Amount field, add a compact toggle:

SOL / USD

Default:
SOL

The active toggle should update the field label:

SOL mode:
Amount (SOL)

USD mode:
Amount (USD)

====================================================
2. QUICK AMOUNT BUTTONS

Update quick amount buttons based on selected unit.

SOL mode:
0.5
1
5
10

USD mode:
$25
$50
$100
$500

Clicking a quick amount should fill the Amount field in the active unit.

====================================================
3. USD TO SOL CONVERSION

Paper trade execution should still submit the expected SOL amount internally.

If user enters USD:

* Convert USD to SOL using the current SOL/USD price already available in the app.
* If SOL/USD price is unavailable, disable Buy/Sell submit and show:
    USD mode unavailable until SOL price loads.

Example:
User enters $100.
SOL price = $200.
Internal trade amount = 0.5 SOL.

Do not send USD amount to the existing trade execution API unless the backend already expects it.

====================================================
4. DISPLAY METRICS UNIT TOGGLE

The Buy/Sell panel currently shows:

Position value
Unrealized P&L

Update these to display in the active unit.

SOL mode:
Position value: 24.09 SOL
Unrealized P&L: -0.91 SOL (-3.65%)

USD mode:
Position value: $4,818.00
Unrealized P&L: -$182.38 (-3.65%)

Percentage should remain unchanged.

If SOL/USD price is unavailable:

* Keep SOL display
* Show a small note that USD display requires SOL price

====================================================
5. MINI TRADE PLANNER SYNC

If the Mini Trade Planner was added to the trade page:

* Use the same SOL/USD unit state if practical.
* When Mini Planner applies a plan to the trade panel, fill the amount using the currently selected Buy/Sell unit.
* Do not break Mini Planner if shared state is messy.
* Prioritize Buy/Sell panel safety and existing trade execution reliability.

====================================================
6. UX REQUIREMENTS

Keep the Buy/Sell panel compact.

Do not make the trading panel taller than necessary.

No horizontal scrolling.

Mobile friendly.

Desktop friendly.

Use existing BlackPebble black/gold trading-terminal styling.

No extra explanatory blocks unless required for errors.

====================================================
7. SAFETY REQUIREMENTS

Do not change actual position math.

Do not change trade fill/slippage logic.

Do not change liquidity blocking rules.

Do not change leaderboard logic.

Do not change portfolio persistence.

Only convert USD input into SOL before sending the existing trade request.

====================================================
8. QA

Verify:

* SOL mode works exactly like before.
* USD mode fills amount field correctly.
* USD quick buttons work.
* USD amount converts to SOL correctly before paper buy.
* USD amount converts to SOL correctly before paper sell if sell supports amount input.
* Position value displays in SOL mode.
* Position value displays in USD mode.
* Unrealized P&L displays in SOL mode.
* Unrealized P&L displays in USD mode.
* Percent P&L stays unchanged.
* Existing buy still works.
* Existing sell still works.
* Mini Planner Apply To Trade still works.
* Typecheck passes.
* Mobile layout does not overflow.
* Desktop layout does not break.

Return:

* files changed
* conversion method used
* confirmation backend/API contract remains compatible
* confirmation SOL mode unchanged
* confirmation USD mode works
* confirmation safe to publish